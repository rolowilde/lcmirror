---
- name: Configure a repository mirror server
  hosts: mirrors
  become: true
  roles:
    - { role: update, tags: update }
    - { role: cron, tags: cron }
    - { role: caddy, tags: caddy }
    - { role: archlinux, when: '"archlinux" in group_names', tags: archlinux }
